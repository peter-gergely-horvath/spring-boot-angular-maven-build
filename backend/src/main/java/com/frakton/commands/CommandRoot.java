package com.frakton.commands;

/**
 * An empty class that marks the root package of commands. Used to create
 * CommandMosaic CommandDispatcherConfiguration, without hard-coding
 * package names a string.
 */
public final class CommandRoot {

    private CommandRoot() {
        // no instances allowed
    }
}
